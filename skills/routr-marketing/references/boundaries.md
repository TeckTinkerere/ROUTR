# routr-marketing boundaries

| Situation | Use instead |
|-----------|-------------|
| Building the landing page UI itself | `routr-frontend` |
| Mobile app store *build*, not the listing copy | `routr-mobile` |
| Analytics/tracking implementation bug | `routr-debug` |
| Share copy for a rendered video | `routr-video` hands off here after render |
