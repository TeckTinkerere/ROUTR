# routr-deploy boundaries

| Situation | Use instead |
|-----------|-------------|
| Writing the code or fix itself | `routr-ship` |
| Verifying the app in a browser after deploy | `routr-qa` |
| Deploy succeeded but broke production behavior | `routr-debug` |
| Migration needs writing, not just running | `routr-database` |
